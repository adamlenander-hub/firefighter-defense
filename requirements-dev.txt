# Extra dependencies needed only to run the checks/tests (not to play the game).
# Install with:  pip3 install -r requirements-dev.txt

pytest
httpx
# Drives the actual game in a headless browser (check.sh step 6) to confirm the
# on-screen rules still match the server and the buttons fit on phones. After
# installing, fetch the browser once:  python3 -m playwright install chromium
playwright
