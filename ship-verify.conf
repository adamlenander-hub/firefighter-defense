# Completion-gate settings for Firefighter Defense. Read by ship-verify.sh.
# These are the project's real, verified values.

MAINLINE=main
CHECK_CMD="sh check.sh"
HEALTH_URL="https://firefighter-defense.onrender.com/health"
# Prove the LIVE site still behaves after a deploy: play it in a real browser against
# the deployed URL (page loads clean, controls fit phones, /health answers).
POST_DEPLOY_CMD="python3 browser_check.py --url https://firefighter-defense.onrender.com"
